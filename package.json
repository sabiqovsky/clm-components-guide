{
  "name": "clm-components-guide",
  "version": "2.0.0",
  "private": true,
  "description": "Xendit Components internal guide — static encrypted HTML",
  "scripts": {
    "build": "node build.js",
    "test": "node test-decrypt.js",
    "preview": "npx serve docs/ -l 3000",
    "dev": "node build.js && npx serve docs/ -l 3000",
    "prebuild": "node test-decrypt.js"
  },
  "devDependencies": {
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
